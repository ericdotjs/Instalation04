<Window x:Class="Ark.InitSettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ark"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Height="150" Width="420">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">Enter HCEEK main directory:</Label>
        <Grid Margin="5 15" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="pathFolderLabel" Grid.Column="0" Grid.Row="0" IsEnabled="False"/>
            <Button x:Name="FolderPathButton" Grid.Column="1" Grid.Row="0" Content="Browse" Width="80" Click="FolderPathButton_Click"/>
        </Grid>
        <Button x:Name="AcceptButton" Click="AcceptButton_Click" Grid.Row="2"  Content="Accept" Width="60" />
    </Grid>
</Window>
